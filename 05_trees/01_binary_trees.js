/**
 * Binary Tree
 * tiap node cuma boleh punya 0-2 nodes
 * 1 child cuma boleh punya 1 parent
 * ada 2 jenis:
 * - perfect binary tree (semua leafnya full, dan gaada nodes yang punya 1 child. bottom layernya kefilled semua) -> lebih efisien
 * - full binary tree (node punya 0 atau 2 children)
 */
